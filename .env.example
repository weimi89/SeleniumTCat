# ═══════════════════════════════════════════════════════════════════════════
# 🌐 Chrome/Chromium 瀏覽器路徑設定
# ═══════════════════════════════════════════════════════════════════════════
# 說明：請複製此檔案為 .env 並依據您的作業系統設定正確路徑
# 安全提醒：設定完成後，請執行 chmod 600 .env 保護敏感資訊

# ───────────────────────────────────────────────────────────────────────────
# 🪟 Windows 路徑範例
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"
# CHROME_BINARY_PATH="C:\Program Files (x86)\Google\Chrome\Application\chrome.exe"

# ───────────────────────────────────────────────────────────────────────────
# 🍎 macOS 路徑範例
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"

# ───────────────────────────────────────────────────────────────────────────
# 🐧 Linux 路徑範例
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="/usr/bin/google-chrome"
# CHROME_BINARY_PATH="/usr/bin/chromium-browser"  # Ubuntu 推薦使用 Chromium

# ───────────────────────────────────────────────────────────────────────────
# 🚗 ChromeDriver 路徑設定（選用）
# ───────────────────────────────────────────────────────────────────────────
# 說明：通常 Selenium 會自動管理 ChromeDriver，但在某些環境可能需要手動指定
# CHROMEDRIVER_PATH="/usr/bin/chromedriver"  # Ubuntu
# CHROMEDRIVER_PATH="C:\chromedriver\chromedriver.exe"  # Windows
# CHROMEDRIVER_PATH="/usr/local/bin/chromedriver"  # macOS

# ═══════════════════════════════════════════════════════════════════════════
# 🔧 快速設定範例
# ═══════════════════════════════════════════════════════════════════════════

# 🐧 Linux/Ubuntu 快速設定（推薦使用 Chromium）
# ───────────────────────────────────────────────────────────────────────────
# 安裝指令：
#   sudo apt update
#   sudo apt install -y chromium-browser chromium-chromedriver
#
# 設定範例：
# CHROME_BINARY_PATH="/usr/bin/chromium-browser"
# CHROMEDRIVER_PATH="/usr/bin/chromedriver"

# 🪟 Windows 快速設定
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"

# 🍎 macOS 快速設定
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"

# ═══════════════════════════════════════════════════════════════════════════
# ⚠️ 安全提醒
# ═══════════════════════════════════════════════════════════════════════════
# 1. 設定完成後，請執行以下指令保護敏感資訊：
#    chmod 600 .env
#
# 2. 確保 .env 已加入 .gitignore，避免提交到版本控制系統
#
# 3. Ubuntu 部署完整指南：docs/technical/ubuntu-deployment-guide.md

# ═══════════════════════════════════════════════════════════════════════════
# 🎯 瀏覽器模式設定
# ═══════════════════════════════════════════════════════════════════════════
# 說明：控制瀏覽器是否以無頭模式執行
# 預設值：true（無頭模式，不顯示瀏覽器視窗）
# 可選值：true（無頭模式）/ false（顯示瀏覽器視窗）

# 設定為 false 以顯示瀏覽器視窗（開發除錯時使用）
# HEADLESS=false

# 設定為 true 以無頭模式執行（生產環境或自動化執行建議使用）
# HEADLESS=true

# ═══════════════════════════════════════════════════════════════════════════
# 📁 下載目錄設定
# ═══════════════════════════════════════════════════════════════════════════
# 說明：設定不同類型檔案的下載目錄
# 預設值：所有檔案下載到 downloads/ 目錄
# 注意：只有需要自訂路徑時才需要設定，預設值適用於大多數情況

# ───────────────────────────────────────────────────────────────────────────
# 📦 貨到付款匯款明細下載目錄
# ───────────────────────────────────────────────────────────────────────────
# PAYMENT_DOWNLOAD_WORK_DIR=downloads

# 自訂範例：按類型分類
# PAYMENT_DOWNLOAD_WORK_DIR=downloads/payment

# 自訂範例：按年月分類
# PAYMENT_DOWNLOAD_WORK_DIR=downloads/2025/01/payment

# ───────────────────────────────────────────────────────────────────────────
# 🧾 運費發票明細下載目錄
# ───────────────────────────────────────────────────────────────────────────
# FREIGHT_DOWNLOAD_WORK_DIR=downloads

# 自訂範例：按類型分類
# FREIGHT_DOWNLOAD_WORK_DIR=downloads/freight

# ───────────────────────────────────────────────────────────────────────────
# 📊 交易明細表下載目錄
# ───────────────────────────────────────────────────────────────────────────
# UNPAID_DOWNLOAD_WORK_DIR=downloads

# 自訂範例：按類型分類
# UNPAID_DOWNLOAD_WORK_DIR=downloads/unpaid

# ═══════════════════════════════════════════════════════════════════════════
# ✅ 已完成下載目錄設定（用於跳過重複下載）
# ═══════════════════════════════════════════════════════════════════════════
# 說明：設定已完成下載的檔案目錄，下載前會檢查此目錄是否已存在相同檔案
# 如果檔案已存在，將跳過下載，避免重複作業
# 注意：如不設定，則每次都會重新下載

# ───────────────────────────────────────────────────────────────────────────
# 📦 貨到付款已完成目錄
# ───────────────────────────────────────────────────────────────────────────
# PAYMENT_DOWNLOAD_OK_DIR=downloads/ok/payment

# ───────────────────────────────────────────────────────────────────────────
# 🧾 運費發票已完成目錄
# ───────────────────────────────────────────────────────────────────────────
# FREIGHT_DOWNLOAD_OK_DIR=downloads/ok/freight

# ───────────────────────────────────────────────────────────────────────────
# 📊 交易明細已完成目錄
# ───────────────────────────────────────────────────────────────────────────
# UNPAID_DOWNLOAD_OK_DIR=downloads/ok/unpaid

# ═══════════════════════════════════════════════════════════════════════════
# 💡 使用提示
# ═══════════════════════════════════════════════════════════════════════════
# 1. 如果不設定這些環境變數，系統將使用預設值
# 2. 預設設定（無頭模式 + downloads/ 目錄）適用於大多數情況
# 3. 僅在需要自訂配置時才取消註解並修改
# 4. 路徑可以是相對路徑（相對於專案根目錄）或絕對路徑
# 5. 系統會自動建立不存在的目錄

# ═══════════════════════════════════════════════════════════════════════════
# 📢 Discord Webhook 通知設定
# ═══════════════════════════════════════════════════════════════════════════
# 說明：設定 Discord Webhook URL 以接收執行結果通知
# 功能：當有密碼安全警告時，自動發送通知到 Discord 頻道
# 取得 Webhook URL：Discord 伺服器設定 → 整合 → Webhook → 建立 Webhook

# ───────────────────────────────────────────────────────────────────────────
# 🔔 Discord Webhook URL
# ───────────────────────────────────────────────────────────────────────────
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# 設定範例（請使用您自己的 Webhook URL）
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1234567890/abcdefghijklmnopqrstuvwxyz

# ───────────────────────────────────────────────────────────────────────────
# 💡 通知內容
# ───────────────────────────────────────────────────────────────────────────
# 當所有帳號處理完成後，系統會自動發送以下通知：
# 1. 執行摘要：總帳號數、成功/失敗數、下載檔案數、執行時間
# 2. 密碼安全警告：列出需要更新密碼的帳號清單（如果有）
#
# 注意：如果不設定此環境變數，系統將不會發送 Discord 通知

# ═══════════════════════════════════════════════════════════════════════════
# 📧 Email SMTP 通知設定
# ═══════════════════════════════════════════════════════════════════════════
# 說明：設定 SMTP 伺服器以接收 Email 執行結果通知
# 功能：與 Discord 通知並行，當有密碼安全警告或執行完成時發送郵件通知
# 注意：如不設定，則不會發送 Email 通知

# ───────────────────────────────────────────────────────────────────────────
# 📮 SMTP 伺服器設定
# ───────────────────────────────────────────────────────────────────────────
# SMTP 伺服器位址
# MAIL_HOST=smtp.gmail.com

# SMTP 伺服器埠號（預設 587）
# MAIL_PORT=587

# SMTP 帳號（通常是您的 Email 地址）
# MAIL_USERNAME=your-email@gmail.com

# SMTP 密碼（Gmail 建議使用應用程式密碼）
# MAIL_PASSWORD=your-app-password

# 加密方式：tls（預設）/ ssl / none
# MAIL_ENCRYPTION=tls

# ───────────────────────────────────────────────────────────────────────────
# 📬 收發信地址
# ───────────────────────────────────────────────────────────────────────────
# 寄件人地址
# MAIL_FROM_ADDRESS=your-email@gmail.com

# 收件人地址（支援多個收件人，用逗號分隔）
# MAIL_TO_ADDRESS=recipient@example.com
# MAIL_TO_ADDRESS=user1@example.com,user2@example.com,user3@example.com

# ───────────────────────────────────────────────────────────────────────────
# 💡 常見 SMTP 設定範例
# ───────────────────────────────────────────────────────────────────────────
# Gmail（需啟用應用程式密碼）：
#   MAIL_HOST=smtp.gmail.com
#   MAIL_PORT=587
#   MAIL_ENCRYPTION=tls
#
# Outlook/Office365：
#   MAIL_HOST=smtp.office365.com
#   MAIL_PORT=587
#   MAIL_ENCRYPTION=tls
#
# Yahoo Mail：
#   MAIL_HOST=smtp.mail.yahoo.com
#   MAIL_PORT=587
#   MAIL_ENCRYPTION=tls

# ═══════════════════════════════════════════════════════════════════════════
# 🔐 安全提醒
# ═══════════════════════════════════════════════════════════════════════════
# 1. Webhook URL 和 SMTP 密碼是敏感資訊，請妥善保管
# 2. 確保 .env 已加入 .gitignore，避免提交到版本控制系統
# 3. 設定完成後，請執行：chmod 600 .env
# 4. 如果 Webhook URL 洩漏，請立即在 Discord 中刪除該 Webhook 並建立新的
# 5. Gmail 建議使用「應用程式密碼」而非帳號密碼